# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

directive @auth on OBJECT | FIELD_DEFINITION

union Meta = ImageMeta | VideoMeta

type Album {
    cover: Medium
    description: String
    id: ID!
    owner: User
    title: String
}

type Count {
    count: Int
}

type File {
    url: String
}

type ImageMeta {
    cameraMake: String
    cameraModel: String
    fNumber: Float
    flash: Int
    height: Int
    iso: Int
    width: Int
}

type Medium {
    dateCreated: DateTime
    dateModified: DateTime
    dateModifiedStatus: DateTime
    dateTaken: DateTime
    description: String
    filenameDisk: String
    filenameDownload: String
    hash: String
    id: ID!
    lat: Float
    lng: Float
    meta: Meta
    mimetype: String
    owner: User
    status: String
    title: String
    uploader: User
}

type Mutation {
    addToAlbum(idAlbum: ID!, media: [ID!]!): [Medium!]!
    createAlbum(album: AlbumInput, media: [ID]): Album
    deleteAlbum(ids: [ID]!): Count
    deleteMedia(ids: [ID]!): [Medium!]!
    emptyTrash: [Medium!]!
    removeFromAlbum(idAlbum: ID!, media: [ID!]!): Album
    rotate(id: ID!): Medium!
    setAlbumCover(idAlbum: ID!, idMedium: ID!): Album
    setMediaStatus(media: [ID]!, status: String): Count
    signIn(mail: String!, password: String!): Token
    signOut: Boolean
    signUp(firstName: String!, lastName: String!, mail: String!, password: String!): Token
    updateAlbumTitle(id: ID!, title: String!): Album
    upload(file: [Upload]!): [File]!
}

type Query {
    album(id: ID!): Album!
    albumMedia(id: ID!): [Medium!]!
    albums: [Album!]!
    media(status: String): [Medium!]
    medium(id: ID!): Medium!
    user(id: ID!): User!
    users: [User!]!
}

type Token {
    accessToken: String!
    refreshToken: String!
}

type User {
    dateCreated: DateTime!
    dateModified: DateTime!
    firstName: String!
    id: ID!
    lastName: String!
    mail: String!
    password: String!
}

type VideoMeta {
    duration: Int
    height: Int
    width: Int
}

"A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar."
scalar DateTime

scalar Upload

input AlbumInput {
    description: String
    id: ID
    idMedium: ID
    title: String
}
