query QAlbumsOfMedium($id: String!) {
    mediumAlbums(id: $id) {
        id
        albums {
            id
            title
            dateCreated
            cover {
                ...FCover
            }
            media {
                id
            }
        }
    }
}
