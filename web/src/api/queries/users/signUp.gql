mutation MSignUp (
    $email: String!
    $password: String!
    $firstName: String!
    $lastName: String!
) {
    signUp(
        email: $email
        password: $password
        firstName: $firstName
        lastName: $lastName
        language: "en-US"
    ) {
        accessToken
        refreshToken
        user {
            ...FUser
        }
    }
}
