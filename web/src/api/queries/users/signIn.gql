mutation MSignIn (
    $email: String!
    $password: String!
) {
    signIn(email: $email, password: $password) {
        accessToken
        refreshToken
        user {
            ...FUser
        }
    }
}
