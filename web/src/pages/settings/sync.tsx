import Layout from '@/layouts/app-layout'
import { Uploader } from '@/components'

const SyncPage = () => {
    return <Layout>
        <section>
            <div>
                <Uploader />
            </div>
        </section>
    </Layout>
}

export default SyncPage
