import Layout from '@/layouts/layout'

const Home = () => {
    return <Layout>
        <section className="flex">
            <div>
                foo
            </div>
        </section>
    </Layout>
}

export default Home
