import { test } from '@playwright/test'
import { globalBeforeEach } from '../support/common'

globalBeforeEach()

test('can upload jpeg', async ({ page }) => {
    // TODO
})

test('can upload mp4', async ({ page }) => {
    // TODO
})

test('can upload zip', async ({ page }) => {
    // TODO
})

test('can upload raw', async ({ page }) => {
    // TODO
})
